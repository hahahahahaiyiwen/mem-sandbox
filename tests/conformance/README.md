# Conformance tests

Conformance tests run the same behavior scenarios through the direct session API and each
framework adapter.

The post-adapter stateful lifecycle scenario, normalized trace requirements, and
relationship between conformance and performance benchmarks are defined in the
[Product Validation and Benchmark Design](../../docs/product-validation/README.md).
